/**
 * Shared Exports for Employee Management Demo
 *
 * Re-exports all shared types and utilities for easy importing
 * across framework implementations.
 */

export * from './data';
export * from './types';

// Note: Styles are exported separately via @demo/shared/styles
// to allow bundler-specific handling of CSS imports
