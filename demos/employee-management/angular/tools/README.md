# Build Tools

These esbuild plugins are **only needed for this monorepo demo**. They redirect `@toolbox-web/*` imports to the local `dist/` folder since we can't install the packages from npm.

**Real-world usage requires no special configuration** - just `npm install @toolbox-web/grid`.
