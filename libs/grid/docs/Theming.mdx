{/* Theming.mdx */}
import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import * as ThemeBuilderStories from './stories/ThemeBuilder.stories';
import * as VariableReferenceStories from './stories/VariableReference.stories';

<Meta title="Grid/Theming" />

# Theming

The `@toolbox-web/grid` component is fully themeable via CSS custom properties. Customize colors, spacing, typography, and more without modifying source code.

## Quick Start

Override CSS custom properties on the grid element:

```css
tbw-grid {
  --tbw-color-bg: #1a1a2e;
  --tbw-color-fg: #eaeaea;
  --tbw-color-border: #16213e;
  --tbw-color-header-bg: #0f3460;
  --tbw-color-row-hover: #1a1a4e;
}
```

## Scaling with Font Size

All sizing uses `em` units, so the grid scales proportionally with `font-size`:

```css
tbw-grid { font-size: 1.25em; }      /* 25% larger */
tbw-grid.compact { font-size: 0.875em; }  /* Compact */
```

## Pre-built Themes

```typescript
import '@toolbox-web/grid/themes/dg-theme-material.css';
```

| Theme | Description |
| --- | --- |
| Standard | Polished, balanced look |
| Material | Material Design 3 inspired |
| Bootstrap | Bootstrap 5 styling |
| Vibrant | Bold purple accents |
| Contrast | High contrast for accessibility |
| Large | Larger fonts/spacing |

Use the theme switcher in the Storybook toolbar to preview!

## Dark Mode Support

The grid uses `light-dark()` for automatic theme support:

```css
tbw-grid { color-scheme: light dark; }  /* Auto-adapt */
tbw-grid { color-scheme: dark; }        /* Force dark */
```

## CSS Cascade Layers

```css
@layer tbw-base, tbw-plugins, tbw-theme;
```

Your unlayered CSS always wins - no `!important` needed.

---

## Interactive Theme Builder

Customize **every CSS variable** with live preview and export your theme as CSS:

<Canvas of={ThemeBuilderStories.Builder} sourceState="none" withToolbar />

---

## CSS Variable Reference

Complete list of all CSS custom properties with defaults:

<Canvas of={VariableReferenceStories.Reference} sourceState="none" />

## See Also

- **[API Reference — CSS Custom Properties](?path=/docs/grid-api--docs#css-custom-properties)** — Quick reference of all CSS variables
- **[Core Grid Component](?path=/docs/grid-core--docs)** — Row and cell styling with `rowClass` and `cellClass`
- **[Accessibility](?path=/docs/grid-dev-accessibility--docs)** — High contrast mode and color contrast requirements
