# grid-vue

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build grid-vue` to build the library.

## Running unit tests

Run `nx test grid-vue` to execute the unit tests via [Vitest](https://vitest.dev/).
