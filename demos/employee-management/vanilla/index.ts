/**
 * Vanilla Employee Management Demo
 *
 * Internal barrel file for the vanilla demo.
 * This is NOT a public API - it's just a demo.
 */

// Re-export shared types used by the demo
export type { DemoConfig, Employee, GridElement } from '@demo/shared';
