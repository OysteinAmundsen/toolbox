/**
 * Shared Calendar Demo — types + data only.
 *
 * Each framework demo (vanilla / Angular / React / Vue) builds its own
 * calendar grid using the same deterministic dataset and types from
 * here. Renderers, grid config, header/toolbar content, pinned-row
 * legend, modal dialog, density observer, keyboard nav, and styles are
 * **NOT** shared — each framework's demo demonstrates that framework's
 * idiomatic way to wire them up.
 */

export * from './data';
export * from './types';
