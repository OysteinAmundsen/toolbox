{/* Introduction.mdx */}
import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Grid/Introduction" />

# <img src="./grid_logo.png" alt="" style={{height: '1.2em', verticalAlign: 'middle', marginRight: '0.3em'}} /> @toolbox-web/grid

A **high-performance, framework-agnostic data grid** web component built with pure TypeScript. No runtime dependencies—just drop it into any project and start rendering data.

## Why Choose @toolbox-web/grid?

We built this grid because we needed something that works everywhere without the baggage of framework-specific implementations. Whether you're building a vanilla JavaScript app, a React dashboard, or an Angular enterprise application, you get the same powerful grid with zero lock-in.

| Feature                    | Description                                                         |
| -------------------------- | ------------------------------------------------------------------- |
| 🚀 **High Performance**    | Virtualized rendering handles 100k+ rows smoothly                   |
| 🔧 **Framework Agnostic**  | Works in vanilla JS, React, Vue, Angular, Svelte—no wrappers needed |
| ⌨️ **Keyboard Navigation** | Full arrow key, Tab, Enter, Escape, Home/End, Page Up/Down support  |
| 🎨 **Themeable**           | CSS custom properties for complete visual customization             |
| 📦 **Plugin Architecture** | Extend with selection, filtering, grouping, and more                |
| 🪶 **Lightweight**         | Zero dependencies, tree-shakeable plugins (~37kB gzipped core)      |

## Quick Start

### Installation

```bash
npm install @toolbox-web/grid
# or use your preferred package manager
yarn add @toolbox-web/grid
pnpm add @toolbox-web/grid
bun add @toolbox-web/grid
```

### Basic Usage

The grid is a standard web component—use it just like any HTML element:

```html
<tbw-grid style="height: 300px;"></tbw-grid>

<script type="module">
  import '@toolbox-web/grid';

  const grid = document.querySelector('tbw-grid');

  grid.columns = [
    { field: 'id', header: 'ID', type: 'number', sortable: true },
    { field: 'name', header: 'Name', sortable: true },
    { field: 'email', header: 'Email' },
  ];

  grid.rows = [
    { id: 1, name: 'Alice', email: 'alice@example.com' },
    { id: 2, name: 'Bob', email: 'bob@example.com' },
    { id: 3, name: 'Carol', email: 'carol@example.com' },
  ];
</script>
```

That's it—no build step required for basic usage. The grid auto-registers the `<tbw-grid>` custom element when you import it.

## Features Overview

### Virtualization

The grid uses row virtualization by default, rendering only visible rows plus a small overscan buffer. This means you can throw a million rows at it and still get smooth 60fps scrolling. The DOM stays lean regardless of dataset size.

### Column Configuration

Columns support extensive configuration options. Here's a taste of what's available:

```typescript
const columns = [
  {
    field: 'name',           // Data field key (required)
    header: 'Full Name',     // Display header
    type: 'string',          // 'string' | 'number' | 'boolean' | 'date' | 'select'
    sortable: true,          // Enable sorting
    resizable: true,         // Enable column resize
    editable: true,          // Enable inline editing (requires EditingPlugin)
    width: 200,              // Initial width in pixels
    minWidth: 100,           // Minimum width when resizing
    format: (val) => val.toUpperCase(),  // Custom formatter
  },
];
```

Check out the [API Reference](../?path=/docs/grid-api--docs) for the complete list of column options.

### Sorting

Click column headers to sort. The grid supports:

- **Single-column sorting** (default) — click a header to sort, click again to reverse
- **Multi-column sorting** — hold Shift and click multiple headers, or use the MultiSort plugin
- **Custom sort comparators** — define your own comparison logic per column

### Inline Editing

Enable editing with the `EditingPlugin` and set `editable: true` on columns:

```typescript
import { EditingPlugin } from '@toolbox-web/grid/plugins/editing';

grid.gridConfig = {
  columns: [
    { field: 'name', editable: true },
    // ...
  ],
  plugins: [new EditingPlugin()],
  editOn: 'dblClick', // or 'click'
};
```

The grid provides:

- **Click** or **double-click** to edit (configurable via `editOn`)
- Built-in editors for text, number, boolean (checkbox), date, and select
- **Enter** to confirm, **Escape** to cancel
- **Tab/Shift+Tab** navigation between editable cells
- Change tracking with commit/reset lifecycle

> **Note:** Editing is opt-in via the `EditingPlugin`. Using `editable: true` without the plugin will throw a helpful error with import instructions.

### Events

The grid emits standard DOM `CustomEvent`s that you can listen to:

| Event           | Description                         |
| --------------- | ----------------------------------- |
| `cell-commit`   | Cell value was edited and committed |
| `row-commit`    | Bulk row edit session completed     |
| `sort-change`   | Sort state changed                  |
| `column-resize` | Column was resized                  |
| `activate-cell` | Cell received focus/activation      |

```typescript
grid.addEventListener('cell-commit', (e) => {
  const { row, field, value, oldValue } = e.detail;
  console.log(`${field} changed from "${oldValue}" to "${value}"`);
});
```

## Plugin Architecture

The core grid is intentionally minimal. Advanced features are delivered through plugins that you can import as needed:

- **SelectionPlugin** — Cell, row, or range selection
- **FilteringPlugin** — Column header filters with custom panels
- **GroupingRowsPlugin** — Hierarchical row grouping with aggregations
- **TreePlugin** — Expandable tree data structures
- **MasterDetailPlugin** — Expandable detail rows
- **PinnedColumnsPlugin** — Sticky columns on left/right edges
- **ExportPlugin** — Export to CSV, Excel, or JSON
- **ClipboardPlugin** — Copy/paste support
- ...and many more

Plugins are tree-shakeable—only what you import ends up in your bundle.

```typescript
import { SelectionPlugin } from '@toolbox-web/grid/plugins/selection';
import { FilteringPlugin } from '@toolbox-web/grid/plugins/filtering';

grid.gridConfig = {
  columns: [...],
  plugins: [
    new SelectionPlugin({ mode: 'row' }),
    new FilteringPlugin({ debounceMs: 300 }),
  ],
};
```

## Next Steps

Ready to dive in? Here's where to go next:

- **[Getting Started](../?path=/docs/grid-getting-started--docs)** — Detailed setup for all frameworks
- **[Demos](../?path=/docs/grid-demos--docs)** — See all features in action
- **[Core Configuration](../?path=/docs/grid-core--docs)** — Full configuration reference
- **[Plugins](../?path=/docs/grid-plugins-overview--docs)** — Extend with selection, filtering, and more
- **[Theming](../?path=/docs/grid-theming--docs)** — Customize the visual appearance
- **[API Reference](../?path=/docs/grid-dev-api-reference--docs)** — Complete property/method/event reference
